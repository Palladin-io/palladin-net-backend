{% include '_header.html.liquid', emailTitle: "Verify your email to receive a shared item" %}
<p style="margin:0 0 16px">Enter this code on the sharing page you already opened. You do not need a Palladin account.</p>
<p style="margin:0 0 40px;font-size:24px;font-weight:700;letter-spacing:4px">{{ code }}</p>
<p style="margin:0;font-size:12px;line-height:1.55;color:#8a8a8a">Request {{ generation }}. Valid until {{ expiresAtUtc }} (UTC). Requesting a new code invalidates the previous code for that session. Never forward this code. If you did not request it, ignore this email.</p>
{% include '_footer.en.html.liquid' %}
