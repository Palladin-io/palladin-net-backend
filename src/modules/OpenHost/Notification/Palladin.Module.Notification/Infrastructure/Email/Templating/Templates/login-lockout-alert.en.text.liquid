We blocked suspicious login attempts

{{ appName }} detected {{ attemptCount }} failed attempts within a {{ windowMinutes }} min window and temporarily blocked further login attempts from this IP address.

Failed attempts: {{ attemptCount }}
Time window: {{ windowMinutes }} min
IP address: {{ ipAddress }}
Blocked at: {{ occurredAt }}
Block expires: {{ lockedUntil }}

Your account was not blocked globally. The restriction applies to further login attempts from the listed IP address.

If you recognize this activity, no action is needed. If you do not, open {{ appName }} directly and review the Audit Log. Also make sure your password is unique and TOTP is enabled.

This alert does not contain an unlock link. The block expires automatically at the time shown above.
{% include '_footer.en.text.liquid' %}
